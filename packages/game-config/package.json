{
  "name": "@at-sevdalisi/game-config",
  "version": "0.1.0",
  "private": true,
  "description": "config/*.config.json dosyalarını tip güvenli şekilde okuyan yükleyici (brief §52 Configuration Sistemi)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
