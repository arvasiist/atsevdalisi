# components

Paylaşılan UI bileşenleri burada toplanacaktır (FAZ 1'den itibaren):
`TopBar`, `HorseCard`, `StatBar`, `RaceCalendarItem`, vb. — bkz.
`docs/GAME_DESIGN.md`.

Bileşenler `src/styles/theme.ts` içindeki tema tokenlarını kullanır; hiçbir
bileşen renk/spacing değerini kendi içinde sabit tutmaz.
